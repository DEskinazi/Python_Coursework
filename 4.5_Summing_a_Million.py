# Make a list of numbers from 1 to 1,000,000 and use min and max to make sure list actually starts at 1 and ends at 1,000,000. Use sum to calculate total
numbers = list(range(1,1000001))

print(min(numbers))
print(max(numbers))
print(sum(numbers))